#include <algorithm>
#include <iostream>
#include <vector>
using namespace std;
struct stu {
    int id, ge, gi, fin;
    vector<int> choice;
};

int main() {
    int n, m, k, quota[105];  // n-申请学生总数 m-学校数量 k-学生志愿数
    scanf("%d%d%d", &n, &m, &k);
    int cnt[110] = {0};  // 所有元素初始化为0
    return 0;
}