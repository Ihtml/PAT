#include <iostream>
#include <vector>
using namespace std;
vector<int> pre, post;
bool isMirror;
int main() {
    int n;
    cin >> n;
    pre.resize(n);
    for (int i = 0; i < n; i++) {
        cin >> pre[i];
    }

    return 0;
}