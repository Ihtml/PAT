#include <iostream>
#include <queue>
#include <vector>
using namespace std;
struct node {
    int val;
    struct node *left, *right;
};

int main() {
    int n, temp;
    cin >> n;
    node* tree = NULL;
    return 0;
}